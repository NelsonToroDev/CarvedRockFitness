# Carved Rock Fitness

The application was created using `dotnet new`:

```bash
dotnet new blazor --framework "net8.0" --empty --no-https
```

test creating a new container image creation on new commit on Azure
